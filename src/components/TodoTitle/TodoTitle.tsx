import React from 'react';
import './TodoTitle.scss';

const TodoTitle = () : JSX.Element =>{
    return (
        <div className='TodoTitle'>
            <div className='TodoTitle-Title'>TodoList with Recoil & Typescript</div>
        </div>
    );
}

export default TodoTitle;